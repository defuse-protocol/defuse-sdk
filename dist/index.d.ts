import * as react_jsx_runtime from 'react/jsx-runtime';
import { Transaction as Transaction$1 } from '@solana/web3.js';
import { Hash, Address } from 'viem';

type SendNearTransaction = (tx: Transaction["NEAR"]) => Promise<{
    txHash: string;
} | null>;

declare enum BaseTokenConvertEnum {
    USD = "usd"
}
type BaseTokenBalance = {
    /** bigint in string */
    balance: string;
    balanceUsd?: string;
    convertedLast?: {
        [key in BaseTokenConvertEnum]: string;
    };
};
type SupportedChainName = "eth" | "near" | "base" | "arbitrum" | "bitcoin" | "solana" | "dogecoin" | "turbochain" | "aurora" | "xrpledger" | "zcash" | "gnosis" | "berachain";
interface FungibleTokenInfo extends Partial<BaseTokenBalance> {
    defuseAssetId: string;
    address: string;
    symbol: string;
    name: string;
    decimals: number;
    icon: string;
    /** @deprecated */
    chainId: string;
    chainIcon: string;
    chainName: SupportedChainName;
    /** @deprecated */
    routes: string[];
}
interface NativeTokenInfo extends Partial<BaseTokenBalance> {
    defuseAssetId: string;
    type: "native";
    symbol: string;
    name: string;
    decimals: number;
    icon: string;
    /** @deprecated */
    chainId: string;
    chainIcon: string;
    chainName: SupportedChainName;
    /** @deprecated */
    routes: string[];
}
type BaseTokenInfo = FungibleTokenInfo | NativeTokenInfo;
/**
 * A virtual aggregation of the same token across multiple blockchains.
 * This is not an on-chain token but a unified view of network-specific tokens
 * with shared properties.
 *
 * The name avoids "NativeMultichainAsset" to clarify that it doesn't represent
 * an actual multichain token, just a virtual abstraction.
 */
interface UnifiedTokenInfo {
    unifiedAssetId: string;
    symbol: string;
    name: string;
    icon: string;
    groupedTokens: BaseTokenInfo[];
}

/* eslint-disable */
/**
 * This file was automatically generated by json-schema-to-typescript.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run json-schema-to-typescript to regenerate this file.
 */

/**
 * This interface was referenced by `DefuseContractABI`'s JSON-Schema
 * via the `definition` "Intent".
 */
type Intent =
  | {
      intent: "add_public_key"
      public_key: string
      [k: string]: unknown
    }
  | {
      intent: "remove_public_key"
      public_key: string
      [k: string]: unknown
    }
  | {
      intent: "invalidate_nonces"
      nonces: string[]
      [k: string]: unknown
    }
  | {
      intent: "transfer"
      memo?: string | null
      receiver_id: AccountId
      tokens: {
        [k: string]: string
      }
      [k: string]: unknown
    }
  | {
      amount: string
      intent: "ft_withdraw"
      memo?: string | null
      /**
       * Message to pass to `ft_transfer_call`. Otherwise, `ft_transfer` will be used. NOTE: No refund will be made in case of insufficient `storage_deposit` on `token` for `receiver_id`
       */
      msg?: string | null
      receiver_id: AccountId
      /**
       * Optionally make `storage_deposit` for `receiver_id` on `token`. The amount will be subtracted from user's NEP-141 `wNEAR` balance. NOTE: the `wNEAR` will not be refunded in case of fail
       */
      storage_deposit?: string | null
      token: AccountId
      [k: string]: unknown
    }
  | {
      intent: "nft_withdraw"
      memo?: string | null
      /**
       * Message to pass to `nft_transfer_call`. Otherwise, `nft_transfer` will be used. NOTE: No refund will be made in case of insufficient `storage_deposit` on `token` for `receiver_id`
       */
      msg?: string | null
      receiver_id: AccountId
      /**
       * Optionally make `storage_deposit` for `receiver_id` on `token`. The amount will be subtracted from user's NEP-141 `wNEAR` balance. NOTE: the `wNEAR` will not be refunded in case of fail
       */
      storage_deposit?: string | null
      token: AccountId
      token_id: string
      [k: string]: unknown
    }
  | {
      amounts: string[]
      intent: "mt_withdraw"
      memo?: string | null
      /**
       * Message to pass to `mt_batch_transfer_call`. Otherwise, `mt_batch_transfer` will be used. NOTE: No refund will be made in case of insufficient `storage_deposit` on `token` for `receiver_id`
       */
      msg?: string | null
      receiver_id: AccountId
      /**
       * Optionally make `storage_deposit` for `receiver_id` on `token`. The amount will be subtracted from user's NEP-141 `wNEAR` balance. NOTE: the `wNEAR` will not be refunded in case of fail
       */
      storage_deposit?: string | null
      token: AccountId
      token_ids: string[]
      [k: string]: unknown
    }
  | {
      amount: string
      intent: "native_withdraw"
      receiver_id: AccountId
      [k: string]: unknown
    }
  | {
      diff: {
        [k: string]: string
      }
      intent: "token_diff"
      memo?: string | null
      referral?: AccountId | null
      [k: string]: unknown
    }
/**
 * NEAR Account Identifier.
 *
 * This is a unique, syntactically valid, human-readable account identifier on the NEAR network.
 *
 * [See the crate-level docs for information about validation.](index.html#account-id-rules)
 *
 * Also see [Error kind precedence](AccountId#error-kind-precedence).
 *
 * ## Examples
 *
 * ``` use near_account_id::AccountId;
 *
 * let alice: AccountId = "alice.near".parse().unwrap();
 *
 * assert!("ƒelicia.near".parse::<AccountId>().is_err()); // (ƒ is not f) ```
 *
 * This interface was referenced by `DefuseContractABI`'s JSON-Schema
 * via the `definition` "AccountId".
 */
type AccountId = string
/**
 * This interface was referenced by `DefuseContractABI`'s JSON-Schema
 * via the `definition` "Deadline".
 */
type Deadline = string
/**
 * See [ERC-191](https://github.com/ethereum/ercs/blob/master/ERCS/erc-191.md)
 *
 * This interface was referenced by `DefuseContractABI`'s JSON-Schema
 * via the `definition` "Erc191Payload".
 */
type Erc191Payload = string
/**
 * This interface was referenced by `DefuseContractABI`'s JSON-Schema
 * via the `definition` "MultiPayload".
 */
type MultiPayload =
  | {
      payload: Nep413Payload
      public_key: string
      signature: string
      standard: "nep413"
      [k: string]: unknown
    }
  | {
      payload: Erc191Payload
      signature: string
      standard: "erc191"
      [k: string]: unknown
    }
  | {
      payload: string
      public_key: string
      signature: string
      standard: "raw_ed25519"
      [k: string]: unknown
    }
  | {
      /**
       * Base64Url-encodded [authenticatorData](https://w3c.github.io/webauthn/#authenticator-data)
       */
      authenticator_data: string
      /**
       * Serialized [clientDataJSON](https://w3c.github.io/webauthn/#dom-authenticatorresponse-clientdatajson)
       */
      client_data_json: string
      payload: string
      standard: "webauthn"
      public_key: string
      signature: string
      [k: string]: unknown
    }
/**
 * This interface was referenced by `DefuseContractABI`'s JSON-Schema
 * via the `definition` "DefusePayload_for_DefuseIntents".
 */
interface DefusePayloadFor_DefuseIntents {
  deadline: Deadline
  /**
   * Sequence of intents to execute in given order. Empty list is also a valid sequence, i.e. it doesn't do anything, but still invalidates the `nonce` for the signer
   */
  intents?: Intent[]
  nonce: string
  signer_id: AccountId
  verifying_contract: AccountId
  [k: string]: unknown
}
/**
 * See [NEP-413](https://github.com/near/NEPs/blob/master/neps/nep-0413.md)
 *
 * This interface was referenced by `DefuseContractABI`'s JSON-Schema
 * via the `definition` "Nep413Payload".
 */
interface Nep413Payload {
  callbackUrl?: string | null
  message: string
  nonce: string
  recipient: string
  [k: string]: unknown
}

type ERC191Message = {
    message: string;
};
type ERC191SignatureData = {
    type: "ERC191";
    signatureData: string;
    signedData: ERC191Message;
};
type NEP413Message = {
    message: string;
    recipient: string;
    nonce: Uint8Array;
    callbackUrl?: string;
};
type NEP413SignatureData = {
    type: "NEP413";
    signatureData: {
        accountId: string;
        /**
         * Base58-encoded signature with curve prefix. Example:
         * ed25519:Gxa24TGbJu4mqdhW3GbvLXmf4bSEyxVicrtpChDWbgga
         */
        publicKey: string;
        /** Base64-encoded signature */
        signature: string;
    };
    /**
     * The exact data that was signed. Wallet connectors may modify this during the signing process,
     * so this property contains the actual data that was signed by the wallet.
     */
    signedData: NEP413Message;
};
type SolanaMessage = {
    message: Uint8Array;
};
type SolanaSignatureData = {
    type: "SOLANA";
    signatureData: Uint8Array;
    signedData: SolanaMessage;
};
type WebAuthnMessage = {
    /** Hash that needs to be signed */
    challenge: Uint8Array;
    /** Underlying payload that will be executed onchain */
    payload: string;
    /** Parsed payload in case UI needs to display it */
    parsedPayload: DefusePayloadFor_DefuseIntents;
};
/** Full response of WebAuthn Login */
type WebAuthnSignature = AuthenticatorAssertionResponse;
type WebAuthnSignatureData = {
    type: "WEBAUTHN";
    signatureData: WebAuthnSignature;
    signedData: WebAuthnMessage;
};
type WalletMessage = {
    ERC191: ERC191Message;
    NEP413: NEP413Message;
    SOLANA: SolanaMessage;
    WEBAUTHN: WebAuthnMessage;
};
type WalletSignatureResult = ERC191SignatureData | NEP413SignatureData | SolanaSignatureData | WebAuthnSignatureData;
type SwapEvent = {
    type: string;
    data: unknown;
    error?: string;
};
type SwappableToken = BaseTokenInfo | UnifiedTokenInfo;
type SwapWidgetProps = {
    theme?: "dark" | "light";
    tokenList: SwappableToken[];
    onEmit?: (event: SwapEvent) => void;
    /**
     * The address (address for EVM, accountId for NEAR, etc) of the user performing the swap.
     * `null` if the user is not authenticated.
     */
    userAddress: string | null;
    userChainType: ChainType | null;
    sendNearTransaction: SendNearTransaction;
    signMessage: (params: WalletMessage) => Promise<WalletSignatureResult | null>;
    onSuccessSwap: (params: {
        amountIn: bigint;
        amountOut: bigint;
        tokenIn: SwappableToken;
        tokenOut: SwappableToken;
        txHash: string;
        intentHash: string;
    }) => void;
    onNavigateDeposit?: () => void;
    initialTokenIn?: SwappableToken;
    initialTokenOut?: SwappableToken;
    /**
     * Optional referral code, used for tracking purposes.
     * Prop is not reactive, set it once when the component is created.
     */
    referral?: string;
};

type ChainType = "near" | "evm" | "solana" | "webauthn";
declare const ChainType: {
    readonly Near: "near";
    readonly EVM: "evm";
    readonly Solana: "solana";
    readonly WebAuthn: "webauthn";
};
type UserInfo = {
    userAddress?: string;
    chainType?: ChainType;
};
type DepositWidgetProps = UserInfo & {
    tokenList: SwappableToken[];
    sendTransactionNear: (tx: Transaction["NEAR"][]) => Promise<string | null>;
    sendTransactionEVM: (tx: Transaction["EVM"]) => Promise<Hash | null>;
    sendTransactionSolana: (tx: Transaction["Solana"]) => Promise<string | null>;
    chainType?: ChainType;
};
type Transaction = {
    NEAR: SendTransactionNearParams;
    EVM: SendTransactionEVMParams;
    Solana: SendTransactionSolanaParams;
};
interface FunctionCallAction {
    type: "FunctionCall";
    params: {
        methodName: string;
        args: object;
        gas: string;
        deposit: string;
    };
}
type Action = FunctionCallAction;
interface SendTransactionNearParams {
    receiverId: string;
    actions: Array<Action>;
}
interface SendTransactionEVMParams {
    from: Address;
    to: Address;
    chainId: number;
    data: Hash;
    value?: bigint;
    gasPrice?: bigint;
    gas?: bigint;
}
interface SendTransactionSolanaParams extends Transaction$1 {
}

declare const DepositWidget: ({ tokenList, userAddress, chainType, sendTransactionNear, sendTransactionEVM, sendTransactionSolana, }: DepositWidgetProps) => react_jsx_runtime.JSX.Element;

declare const SwapWidget: ({ tokenList, userAddress, userChainType, sendNearTransaction, signMessage, onSuccessSwap, onNavigateDeposit, initialTokenIn, initialTokenOut, referral, }: SwapWidgetProps) => react_jsx_runtime.JSX.Element;

type WithdrawWidgetProps = UserInfo & {
    tokenList: (BaseTokenInfo | UnifiedTokenInfo)[];
    signMessage: (params: WalletMessage) => Promise<WalletSignatureResult | null>;
    sendNearTransaction: SendNearTransaction;
    /**
     * Optional referral code, used for tracking purposes.
     * Prop is not reactive, set it once when the component is created.
     */
    referral?: string;
};

declare const WithdrawWidget: (props: WithdrawWidgetProps) => react_jsx_runtime.JSX.Element;

type SignMessage = (params: WalletMessage) => Promise<WalletSignatureResult | null>;

type OtcMakerWidgetProps = {
    /** List of available tokens for trading */
    tokenList: (BaseTokenInfo | UnifiedTokenInfo)[];
    /** User's wallet address */
    userAddress: string | null | undefined;
    userChainType: ChainType | null | undefined;
    /** Initial tokens for pre-filling the form */
    initialTokenIn?: BaseTokenInfo | UnifiedTokenInfo;
    initialTokenOut?: BaseTokenInfo | UnifiedTokenInfo;
    /** Sign message callback */
    signMessage: SignMessage;
    /** Function to generate a shareable trade link */
    generateLink: (multiPayload: MultiPayload) => string;
    /** Theme selection */
    theme?: "dark" | "light";
};

declare function OtcMakerWidget(props: OtcMakerWidgetProps): react_jsx_runtime.JSX.Element;

type OtcTakerWidgetProps = {
    multiPayload: MultiPayload | string;
    /** List of available tokens for trading */
    tokenList: (BaseTokenInfo | UnifiedTokenInfo)[];
    /** User's wallet address */
    userAddress: string | null | undefined;
    userChainType: ChainType | null | undefined;
    /** Sign message callback */
    signMessage: SignMessage;
    /** Theme selection */
    theme?: "dark" | "light";
};
declare function OtcTakerWidget(props: OtcTakerWidgetProps): react_jsx_runtime.JSX.Element;

declare function isBaseToken(token: BaseTokenInfo | UnifiedTokenInfo): token is BaseTokenInfo;
declare function isUnifiedToken(token: BaseTokenInfo | UnifiedTokenInfo): token is UnifiedTokenInfo;

/**
 * A branded string type representing a Defuse user ID.
 * The brand prevents accidental mixing with regular strings in TypeScript.
 */
type DefuseUserId = string & {
    __brand: "DefuseAccountId";
};

type WithdrawParams = {
    type: "to_near";
    amount: bigint;
    tokenAccountId: string;
    receiverId: string;
    storageDeposit: bigint;
} | {
    type: "via_poa_bridge";
    amount: bigint;
    tokenAccountId: string;
    destinationAddress: string;
    destinationMemo: string | null;
} | {
    type: "to_aurora_engine";
    amount: bigint;
    tokenAccountId: string;
    auroraEngineContractId: string;
    destinationAddress: string;
};

interface SignerCredentials {
    /** The credential (blockchain address or WebAuthn public key) that will sign or has signed the intent */
    credential: string;
    /** The type of credential (chain or authentication method) */
    credentialType: ChainType;
}
/**
 * Serializes a signed intent into the protocol's wire format
 * Transforms both signature and message data into the standardized
 * encoding expected by the Near Intents Protocol
 *
 * @param signature The signature result from the wallet
 * @param credentials The signer's credentials
 * @returns Intent data serialized in protocol wire format
 */
declare function formatSignedIntent(signature: WalletSignatureResult, credentials: SignerCredentials): MultiPayload;
/**
 * Converts a user's blockchain address or WebAuthn credential to a standardized Near Intents protocol ID
 * @param credentials The signer's credentials
 * @returns A standardized Near Intents protocol user ID
 */
declare function formatUserIdentity(credentials: SignerCredentials): DefuseUserId;

interface IntentMessageConfig {
    /**
     * User identifier either as DefuseUserId or SignerCredentials
     * If SignerCredentials is provided, it will be converted to DefuseUserId
     */
    signerId: DefuseUserId | SignerCredentials;
    /**
     * Optional deadline timestamp in milliseconds
     * @default 5 minutes from now
     */
    deadlineTimestamp?: number;
    /**
     * Optional nonce for tracking
     * @default random nonce
     */
    nonce?: Uint8Array;
    /**
     * Optional referral code for tracking
     */
    referral?: string;
}
type WithdrawIntentMessageConfig = WithdrawParams;
/**
 * Creates an intent message for token swaps
 * @param swapConfig Array of [tokenAddress, amount] tuples representing the swap
 * @param options Message configuration options
 * @returns Intent message ready to be signed by a wallet
 */
declare function createSwapIntentMessage(swapConfig: [string, bigint][], options: IntentMessageConfig): WalletMessage;
/**
 * Creates an intent message for withdrawal operations
 * @param withdrawConfig Withdrawal-specific configuration (target chain, amount, destination)
 * @param options General message options (signer, deadline, etc.)
 * @returns Intent message ready to be signed by a wallet
 */
declare function createWithdrawIntentMessage(withdrawConfig: WithdrawIntentMessageConfig, options: IntentMessageConfig): WalletMessage;
/**
 * Creates an empty intent message that can be used for testing connections
 * @param options Message configuration options
 * @returns Intent message ready to be signed by a wallet
 */
declare function createEmptyIntentMessage(options: IntentMessageConfig): WalletMessage;

export { type BaseTokenInfo, ChainType, type DefuseUserId, DepositWidget, type IntentMessageConfig, OtcMakerWidget, OtcTakerWidget, type SignerCredentials, SwapWidget, type UnifiedTokenInfo, type WithdrawIntentMessageConfig, WithdrawWidget, createEmptyIntentMessage, createSwapIntentMessage, createWithdrawIntentMessage, formatSignedIntent, formatUserIdentity, isBaseToken, isUnifiedToken };
